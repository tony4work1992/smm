import { IPathSelectProps } from "../atoms/IPathSelectProps";

export interface IFieldPathMapperProps {
  index: number;
  fromModel: IPathSelectProps;
  toModel: IPathSelectProps;

  // TODO: add more params if need
}
